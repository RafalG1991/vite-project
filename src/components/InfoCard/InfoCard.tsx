export type InfoCardProps = {
  title: string;
  description: string;
  label: string;
}

export const InfoCard = ({title, description, label}: InfoCardProps) => {
  return (
    <div>

    </div>
  )
}